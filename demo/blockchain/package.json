{
  "name": "relay-blockchain",
  "version": "1.0.0",
  "description": "Hardhat deployment for Relay contracts",
  "devDependencies": {
    "hardhat": "^2.17.0",
    "@nomiclabs/hardhat-ethers": "^2.2.0",
    "ethers": "^5.7.2"
  },
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  }
}
